const RenderOne= ({setup, punchline}) => {
    return (  
        <div>
            <p><strong>{setup}</strong></p>
            <p>{punchline}</p>
        </div>
    );
}
 
export default RenderOne ;
